<Page
    x:Class="Genesha.Views.WhiteboardListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Genesha.ViewModels">
    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Whiteboards" FontSize="24" FontWeight="SemiBold" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
            <AutoSuggestBox x:Name="SearchBox" Width="260" PlaceholderText="Search whiteboards…"
                             QueryIcon="Find" TextChanged="SearchBox_TextChanged" />
            <Button Content="New Whiteboard" Click="NewWhiteboardButton_Click" />
        </StackPanel>

        <Grid Grid.Row="2">
            <TextBlock x:Name="EmptyStateText"
                       Text="No whiteboards yet. Create one to get started."
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="Collapsed" />
            <ListView x:Name="BoardsList"
                      SelectionMode="None" IsItemClickEnabled="True"
                      ItemClick="BoardsList_ItemClick">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="vm:WhiteboardListItemVm">
                        <Grid Padding="12,10" ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Spacing="2">
                                <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                                <TextBlock Text="{x:Bind UpdatedAtText}" FontSize="11"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <Button Grid.Column="1" VerticalAlignment="Center" Background="Transparent" BorderThickness="0"
                                    Tag="{x:Bind}" Click="BoardMenuButton_Click" ToolTipService.ToolTip="More options">
                                <FontIcon Glyph="&#xE712;" FontSize="14" />
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
